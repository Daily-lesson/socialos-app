# socialos-app (deployment mirror)

This repo contains **only the built, runnable front-end** of SocialOS, served
via GitHub Pages. It is a generated mirror — the source of truth, docs, and
history live in a separate private repository. Do not edit files here directly;
they are overwritten on each deploy.

SocialOS is a personal AI social media manager PWA.
